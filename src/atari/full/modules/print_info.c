#include <stdio.h>
#include "fn_data.h"

// This is debug only. pulling in sprintf adds 2k to runtime
void print_info() {
    // char b1[16];
    // sprintf(b1, "CHBAS: %d", OS.chbas);
    // put_s(15, 5, b1);
    // sprintf(b1, "bank count: %d", bank_count);
    // put_s(10, 6, b1);
}
