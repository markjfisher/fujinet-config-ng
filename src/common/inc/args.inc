.struct SaveDeviceChoiceArgs
    device_slot     .byte
    mode            .byte
.endstruct

.struct ElipsizeArgs
    src             .word
    dst             .word
    max             .byte
.endstruct
