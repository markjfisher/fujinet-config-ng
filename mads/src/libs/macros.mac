.macro pushAX
    pha
    txa
    pha
.endm

.macro pullAX
    pla
    tax
    pla
.endm

.macro set_sio_defaults
    mva #$70 DDEVIC
    mva #$01 DUNIT
    mva #$0f DTIMLO  ; 15 second timeout
.endm

.macro call_siov
    jsr SIOV
.endm